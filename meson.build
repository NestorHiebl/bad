project(
  'bad',
  'c',
  default_options: [
    'debug=true',
    'warning_level=everything',
    'werror=true',
    'buildtype=debugoptimized'
  ]
)

inc = include_directories('inc')

subdir('inc')
subdir('src')
subdir('test')

